#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(){
    int angle;
    printf("Set angle: \n");
    scanf("%d",&angle);
    printf("Sine: %f \n", sin(angle));
    printf("Cosine: %f \n", cos(angle));
    printf("Tangent: %f \n", tan(angle));
    //printf("Cotangent: %f \n", cotan(angle));

}