#include <stdio.h>
#include <stdlib.h>

//void addElement(int n){'
//     struct Node *temp
  //  if (head == NULL){
//        head = (struct Node) malloc(sizeof(struct Node));
//            head -> data = data;
//              head -> next = NULL;                    //REALLY IMPOTANT!!!!!
 //   }
 /*   else{
            temp = head
        }
      while(temp -> next != NULL){

      }
 */
//}

